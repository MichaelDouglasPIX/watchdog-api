apiVersion: 1

providers:
  - name: 'default'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
    allowUiUpdates: true
    folder: 'Watchdog API'
